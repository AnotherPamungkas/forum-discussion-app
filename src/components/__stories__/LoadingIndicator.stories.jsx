import React from 'react';
import LoadingIndicator from '../LoadingIndicator';

export default {
  title: 'Components/LoadingIndicator',
  component: LoadingIndicator,
};

export const Default = () => React.createElement(LoadingIndicator);
